{
   "version": "2.0.0",
   "tasks": [
     {
       "label": "build",
       "type": "shell",
       "command": "make",  // Adjust this if your build command is different
       "group": {
         "kind": "build",
         "isDefault": true
       }
     }
   ]
 }
 